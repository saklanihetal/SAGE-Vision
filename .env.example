# Copy this file to `.env` (this directory or rpi_edge/) and fill in the value.
# `.env` is git-ignored; this template is committed so the repo documents which
# variables the node needs without exposing the secret.

# ThingSpeak write API key — used by `--cloud` to upload telemetry.
THINGSPEAK_API_KEY=
