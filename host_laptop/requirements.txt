# laptop_logger.py uses only the Python standard library (socket, struct,
# datetime) — no third-party packages are required. This file is kept so the
# documented `pip install -r host_laptop/requirements.txt` step stays valid.
